# Python packages used by ImageEXIFStyler.
# Versions are pinned from the current `ies` conda environment.

# Runtime
PySide6==6.10.3
PySide6_Addons==6.10.3
PySide6_Essentials==6.10.3
shiboken6==6.10.3
Pillow==11.3.0
Jinja2==3.1.6
MarkupSafe==3.0.3

# Optional runtime support
# pillow_heif enables HEIC input. The packaged app excludes it by default
# unless build_exe.ps1 is run with -IncludeHeif.
pillow_heif==1.1.1

# loguru is optional at runtime; the project falls back to Python logging
# when loguru is unavailable. It is excluded from the packaged app by default.
loguru==0.7.3
colorama==0.4.6
win32_setctime==1.2.0

# Packaging
pyinstaller==6.19.0
pyinstaller-hooks-contrib==2026.4
altgraph==0.17.5
packaging==26.0
pefile==2024.8.26
pywin32-ctypes==0.2.3
